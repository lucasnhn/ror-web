# These are fake environment variables intended only for use
# by the GitHub workflow during project build operations.
# They should not be used in any other context.
AUTH_SECRET="totally-insecure-auth-secret"
AUTH_ISSUER=http://localhost:3001
AUTH_CLIENT_ID=ABC
AUTH_CLIENT_SECRET=DEF
AUTH_TRUST_HOST=true
AUTH_AUDIENCE=some-audience
NEXT_PUBLIC_ROR_API_URL=http://localhost:3002
NEXT_PUBLIC_MOCKING_ENABLED=false
