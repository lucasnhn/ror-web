apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ror-web.fullname" . }}-test-connection"
  labels:
    {{- include "ror-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ror-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
