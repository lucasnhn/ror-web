apiVersion: v1
kind: Service
metadata:
  name: {{ include "ror-web.fullname" . }}
  labels:
    {{- include "ror-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ror-web.selectorLabels" . | nindent 4 }}
